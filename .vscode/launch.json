{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug check1",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/tests/reassembler_cap_sanitized",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ]
}
